Name: Manual Workflow

on:
  workflow_dispatch:
    inputs:
      name:
        description: test manual workflow
        type: string
        default: 'test'

jobs:
  manual:
    runs-on: ubuntu-slim

    steps:
      - name: Greetings
        run: echo "Hello World!"
